# Dasar HTML
> Dalam bab ini kita akan menunjukkan beberapa contoh HTML dasar. 
>
> Jangan khawatir jika kami menggunakan tag yang belum Anda pelajari.

## Elemen Wajib Dalam Documen HTML
Ada beberapa elemen yang wajib ada dalam dokumen HTML. 
Jika tidak ada elemen tersebut maka documen HTML akan error menurut validator W3C.

Berikut ini daftar elemen yang wajib ada dalam documen HTML:

* ```<!DOCTYPE html>```: mendefinisikan bahwa dokumen ini adalah dokumen HTML5
* ```<html>```: adalah elemen utama dari halaman HTML
* ```<head>```: berisi informasi meta tentang halaman HTML
* ```<title>```: menentukan judul untuk halaman HTML;
* ```<body>```: mendefinisikan isi dari dokumen HTML, dan merupakan wadah untuk semua konten yang terlihat, 


## Heading 

Heading dalam HTML didefinisikan dengan tag ```<h1>``` to .```<h6>```

```<h1>``` mendefinisikan judul yang paling penting. Sedangkan ```<h6>``` mendefinisikan heading yang paling tidak penting: 
import Trycode from '../../component/trycode'

<div >
import Trycode from '../../component/trycode'


<Trycode code_source="https://www.w3schools.com/html/tryit.asp?filename=tryhtml_basic_headings">
    ```html
<h1> Heading Pertama </h1> 
<h2> Heading Kedua </h2> 
<h3> Heading Ketika </h3> 
<h4> Heading keempat </h4> 
<h5> Heading Kelima </h5> 
    ```
    </Trycode>
</div>

## Paragraf 

Paragraf dalam HTML didefinisikan dengan ```<p>``` sebagai tag:
<div >
<Trycode code_source="https://www.w3schools.com/html/tryit.asp?filename=tryhtml_basic_paragraphs">
    ```html
    <p>This is a paragraph.</p>
    <p>This is another paragraph.</p>
    <p>This is another another paragraph.</p>
    
    ```
    </Trycode>
    
</div>

## Link  

Link dalam HTML didefinisikan dengan tag ```<a>```.

Sedangkan tujuan tautan ditentukan dalam atribut ```href``` . 

Atribut digunakan untuk memberikan informasi tambahan tentang elemen HTML.

<div >
<Trycode code_source="https://www.w3schools.com/html/tryit.asp?filename=tryhtml_basic_link">
    ```html
<a href="https://www.w3schools.com">This is a link</a>

    ```
    </Trycode>
</div>

## Gambar   

Gambar dalam HTML didefinisikan dengan tag ```<img>```.
Sedangkan file sumber ditentukan dalam atribut ```src```
Elemen ```<img>``` memiliki atribut lain seperti teks alternatif (```alt```), ```width```, dan ```height``` 
<div >
<Trycode code_source="https://www.w3schools.com/html/tryit.asp?filename=tryhtml_basic_img">
    ```html
<img src="w3schools.jpg" alt="W3Schools.com" width="104" height="142">

    ```
    </Trycode>
</div>

